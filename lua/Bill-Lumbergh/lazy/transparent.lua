return {
  "xiyaowong/transparent.nvim",   -- ← replace with the actual repo
  lazy = false,                -- This forces it to load at startup
  priority = 1000,             -- Optional: load earlier (good for colors/UI plugins)
}
